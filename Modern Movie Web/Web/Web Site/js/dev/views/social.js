(function (window, undefined) {

    "use strict";

    movieApp.fn.socialView = View.extend({


        onload: function () {
            this.setMainTitle("modern web movies social networks");
        }

    });

}(window));